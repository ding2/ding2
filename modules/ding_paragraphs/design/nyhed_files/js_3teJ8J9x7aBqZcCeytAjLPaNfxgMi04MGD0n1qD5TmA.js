Drupal.locale = { 'pluralFormula': function ($n) { return Number(($n!=1)); }, 'strings': {"":{"Edit":"Redig\u00e9r","Not published":"Ikke udgivet","Media browser":"Mediebrowser","Disabled":"Deaktiveret","Enabled":"Aktiveret","All":"Alle","Yes":"Ja","No":"Nej","Monday":"Mandag","Tuesday":"Tirsdag","An AJAX HTTP error occurred.":"Der opstod en AJAX HTTP-fejl.","HTTP Result Code: !status":"HTTP resultatkode: !status","An AJAX HTTP request terminated abnormally.":"En AJAX HTTP-foresp\u00f8rgsel afsluttede p\u00e5 unormal vis.","Debugging information follows.":"Information til fejls\u00f8gning f\u00f8lger.","Path: !uri":"Sti: !uri","StatusText: !statusText":"StatusText: !statusText","ResponseText: !responseText":"ResponseText: !responseText","ReadyState: !readyState":"ReadyState: !readyState","Searching please wait...":"S\u00f8ger - vent venligst..","Cancel":"Annull\u00e9r","Mon":"man","@name Alert":"@name Advarsel om time out","Please wait...":"Vent venligst\u2026","Thursday":"Torsdag","Friday":"Fredag","Thu":"tor","Wednesday":"Onsdag","Configure":"Konfigur\u00e9r","Show shortcuts":"Vis genveke","Hide shortcuts":"Gem genveje","(active tab)":"(aktiv fane)","available":"Tilg\u00e6ngelig","on loan":"Udl\u00e5nt","unavailable":"Kan ikke udl\u00e5nes","Holdings available on the shelf":"Se alle eksemplarer","Hide":"Skjul","Show":"Vis","Select all rows in this table":"V\u00e6lg alle r\u00e6kker i tabellen","Deselect all rows in this table":"Frav\u00e6lg alle r\u00e6kker i tabellen","Re-order rows by numerical weight instead of dragging.":"Sort\u00e9r r\u00e6kker med numeriske v\u00e6gte i stedet for at tr\u00e6kke dem.","Show row weights":"Vis r\u00e6kkev\u00e6gte","Hide row weights":"Skjul r\u00e6kkev\u00e6gte","Drag to re-order":"Tr\u00e6k for at (om)sortere","Changes made in this table will not be saved until the form is submitted.":"\u00c6ndringer i tabellen bliver ikke gemt, f\u00f8r du indsender formularen.","The selected file %filename cannot be uploaded. Only files with the following extensions are allowed: %extensions.":"Den valgte fil %filename kan ikke uploades. Kun filer med f\u00f8lgende filendelser er tilladt: %extensions.","Autocomplete popup":"Autocomplete popup","Searching for matches...":"S\u00f8ger efter resultater...","New revision":"Ny version","No revision":"Ingen version","By @name on @date":"Af @name, @date","By @name":"Af @name","Automatic alias":"Automatisk alias","Alias: @alias":"Alias: @alias","No alias":"Intet alias","@number comments per page":"@number kommentarer pr. side","Done":"F\u00e6rdig","There is nothing in your media library. Select the Upload tab above to add a file.":"Dit medie-bibliotek er tomt. V\u00e6lg upload-fanen ovenfor for at tilf\u00f8je en fil.","Error getting media.":"Fejl ved hentning af media.","Submit":"Indsend","Cannot continue, nothing selected":"Kan ikke forts\u00e6tte, intet valgt","This permission is inherited from the authenticated user role.":"Denne tilladelse er arvet fra den godkendte brugerrolle.","Insert this token into your form":"Inds\u00e6t dette token i din formular","First click a text field to insert your tokens into.":"Klik p\u00e5 et tekstfelt som tokens skal s\u00e6ttes ind i.","OK":"O.k.","Not in menu":"Ikke i menu","Show more filters":"Vis flere","Show less filters":"Vis f\u00e6rre filtre","Show more":"Vis flere","Show less":"Vis f\u00e6rre","Next":"N\u00e6ste","closed":"lukket","More":"Mere","Save":"Gem","Sunday":"S\u00f8ndag","Saturday":"L\u00f8rdag","This field is required.":"Dette felt er obligatorisk.","Prev":"Forrige","Tue":"tir","Wed":"ons","Fri":"fre","Sat":"l\u00f8r","Sun":"s\u00f8n","January":"Januar","February":"Februar","March":"Marts","April":"April","May":"maj","June":"Juni","July":"Juli","August":"August","September":"September","October":"Oktober","November":"November","December":"December","Today":"I dag","Jan":"jan","Feb":"feb","Mar":"mar","Apr":"apr","Jun":"jun","Jul":"jul","Aug":"aug","Sep":"sep","Oct":"okt","Nov":"nov","Dec":"dec","Su":"S\u00f8","Mo":"Ma","Tu":"Ti","We":"On","Th":"To","Fr":"Fr","Sa":"L\u00f8","mm\/dd\/yy":"mm\/dd\/yy","Limit search to":"Afgr\u00e6ns din s\u00f8gning","Available":"Tilg\u00e6ngelig","Opening hours":"\u00c5bningstider","Select all":"Mark\u00e9r alt","If you switch views, you will lose your selection.":"Hvis du skifter views mister du dine valg.","Restore":"Gendan","Discard changes":"Fortryd \u00e6ndringer","Delete instance":"Slet instans","Not reservable":"Kan ikke reserveres","No flags":"Ingen flag","On by default with opt out":"Sl\u00e5et til som standard med opt out.","Off by default with opt in":"Sl\u00e5et fra som standard med opt in.","Not tracked":"Ikke sporet","One domain with multiple subdomains":"\u00c9t dom\u00e6ne med flere subdom\u00e6ner","Multiple top-level domains":"Flere top-level dom\u00e6ner","All pages with exceptions":"Alle sider med undtagelser","Excepted: @roles":"Undtaget: @roles","A single domain":"Et enkelt dom\u00e6ne","Universal web tracking opt-out":"Universel afmelding af web tracking","No privacy":"Ingen beskyttelse af personlige oplysninger","@items enabled":"@items aktiveret","New":"Ny","Loading token browser...":"Indl\u00e6ser token-browser...","Available tokens":"Tilg\u00e6ngelige symboler","Downloads":"Downloads","An error occurred":"Der opstod en fejl","No nodes in this queue.":"Ingen indholdselementer i denne k","We have !x copies and !y reservations for this material.":"Bibliotekerne har indk\u00f8bt !x eksemplar(er). Der er !y reservering(er)","Scheduled for publishing":"Planlagt til publicering","Scheduled for unpublishing":"Planlagt til afpublicering","Not scheduled":"Ikke planlagt","Ignore":"Ignor\u00e9r","close":"Luk","Can be obtained:":"Kan fremskaffes","Close":"Luk","Limit search results":"Afgr\u00e6ns din s\u00f8gning","Reservable":"Reserveringsk\u00f8","Remove all selected":"Fjern valgte","Can be obtained":"Kan fremskaffes","No redirects":"Ingen omdirigeringer"," AND ":" AND ","-@count":"-@count","No results":"Din s\u00f8gning gav desv\u00e6rre ikke noget resultat.","Not available":"Ikke tilg\u00e6ngelig","\u0022Changes made in this table will not be saved until the form is \u0022submitted.":"\u00c6ndringer i tabellen bliver ikke gemt, f\u00f8r du indsender formularen.","\u0022The selected file %filename cannot be uploaded. Only files with the \u0022following extensions are allowed: %extensions.":"\u0022Den valgte fil %filename kan ikke uploades. Kun filer med f\u00f8lgende \u0022filendelser er tilladt: %extensions.","\u0022There is nothing in your media library. Select the Upload tab above to \u0022add a file.":"\u0022Dit medie-bibliotek er tomt. V\u00e6lg upload-fanen ovenfor for at \u0022tilf\u00f8je en fil.","List additional actions":"Vis yderligere handlinger","Using defaults":"Bruger standardindstillinger","(cropped)":"(besk\u00e5ret)","\u0022It appears that some of the images could not be loaded for cropping, \u0022please try again in another browser.":"Nogle af billederne kunne ikke besk\u00e6res. Pr\u00f8v venligst igen i en anden browser."}} };;
/**
 * @file
 *
 * Provide event nodes/pages with ticket info from Place2book
 */
(function ($) {

  Drupal.behaviors.ding_place2book = {
    attach: function (context, settings) {
      $('.place2book-ticketinfo').once('place2book-button', function() {
        var request = $.ajax({
          url: Drupal.settings.basePath + 'ding/place2book/ticketinfo/' + this.value,
          type: 'POST',
          dataType: 'json',
          success: ding_place2book_insert,
        });
      });
    }
  };

  /**
   * Replace placeholder with place2book data.
   */
  var ding_place2book_insert = function(ding_place2book) {
    $(".place2book-ticketinfo[data-ticket='" + ding_place2book.nid + "']").replaceWith(ding_place2book.markup);
  };
 
})(jQuery);
;
(function ($) {

/**
 * A progressbar object. Initialized with the given id. Must be inserted into
 * the DOM afterwards through progressBar.element.
 *
 * method is the function which will perform the HTTP request to get the
 * progress bar state. Either "GET" or "POST".
 *
 * e.g. pb = new progressBar('myProgressBar');
 *      some_element.appendChild(pb.element);
 */
Drupal.progressBar = function (id, updateCallback, method, errorCallback) {
  var pb = this;
  this.id = id;
  this.method = method || 'GET';
  this.updateCallback = updateCallback;
  this.errorCallback = errorCallback;

  // The WAI-ARIA setting aria-live="polite" will announce changes after users
  // have completed their current activity and not interrupt the screen reader.
  this.element = $('<div class="progress" aria-live="polite"></div>').attr('id', id);
  this.element.html('<div class="bar"><div class="filled"></div></div>' +
                    '<div class="percentage"></div>' +
                    '<div class="message">&nbsp;</div>');
};

/**
 * Set the percentage and status message for the progressbar.
 */
Drupal.progressBar.prototype.setProgress = function (percentage, message) {
  if (percentage >= 0 && percentage <= 100) {
    $('div.filled', this.element).css('width', percentage + '%');
    $('div.percentage', this.element).html(percentage + '%');
  }
  $('div.message', this.element).html(message);
  if (this.updateCallback) {
    this.updateCallback(percentage, message, this);
  }
};

/**
 * Start monitoring progress via Ajax.
 */
Drupal.progressBar.prototype.startMonitoring = function (uri, delay) {
  this.delay = delay;
  this.uri = uri;
  this.sendPing();
};

/**
 * Stop monitoring progress via Ajax.
 */
Drupal.progressBar.prototype.stopMonitoring = function () {
  clearTimeout(this.timer);
  // This allows monitoring to be stopped from within the callback.
  this.uri = null;
};

/**
 * Request progress data from server.
 */
Drupal.progressBar.prototype.sendPing = function () {
  if (this.timer) {
    clearTimeout(this.timer);
  }
  if (this.uri) {
    var pb = this;
    // When doing a post request, you need non-null data. Otherwise a
    // HTTP 411 or HTTP 406 (with Apache mod_security) error may result.
    $.ajax({
      type: this.method,
      url: this.uri,
      data: '',
      dataType: 'json',
      success: function (progress) {
        // Display errors.
        if (progress.status == 0) {
          pb.displayError(progress.data);
          return;
        }
        // Update display.
        pb.setProgress(progress.percentage, progress.message);
        // Schedule next timer.
        pb.timer = setTimeout(function () { pb.sendPing(); }, pb.delay);
      },
      error: function (xmlhttp) {
        pb.displayError(Drupal.ajaxError(xmlhttp, pb.uri));
      }
    });
  }
};

/**
 * Display errors on the page.
 */
Drupal.progressBar.prototype.displayError = function (string) {
  var error = $('<div class="messages error"></div>').html(string);
  $(this.element).before(error).hide();

  if (this.errorCallback) {
    this.errorCallback(this);
  }
};

})(jQuery);
;
/**
 * @file
 * Rating jquery widget
 *
 * This widget allows users to rate library materials.
 */

(function ($) {
  "use strict";

  $.widget("ding.rating", {
    options: {
      'average': 1,
      'submitted': false,
    },

    /**
     * Constructor.
     */
    _create: function() {
      var self = this;

      this.element
        // Add a class for theming.
        .addClass('ding-rating')
        // Prevent double click to select text.
        .disableSelection();
      if(this.options.submitted === false) {
        $('.js-rating-symbol', this.element)
          .hover(this.starMouseIn, this.starMouseOut)
          .bind('click', function (evt) {
            evt.preventDefault();

            var $this = $(this);
            self.sendRating(
              $this.parent().attr('data-ding-entity-rating-path'),
              $this.index() + 1,
              function () {
                $this.addClass('submitted').parent('.ding-entity-rating').addClass('has-submission');
                $this.prevAll('.js-rating-symbol').addClass('submitted');
                $this.nextAll('.js-rating-symbol').removeClass('submitted has-sub');
              }
            );
          });

        $('.ding-entity-rating-clear', this.element).bind('click', function (evt) {
          evt.preventDefault();

          var $this = $(this);
          self.sendRating(
            $this.parent().attr('data-ding-entity-rating-path'),
            0,
            function () {
              $this.parent('.ding-entity-rating')
                .removeClass('has-submission')
                .children('.js-rating-symbol').removeClass('submitted has-sub');
            }
          );
        });
      }
      this._refresh();
    },

    /**
     * Called when created, and later when changing options.
     */
    _refresh: function() {
      this._trigger( "change" );
    },

    /**
     * On mouse in.
     */
    starMouseIn: function() {
      var $this = $(this);
      $this.parent().children('.submitted').addClass('has-sub').removeClass('submitted');
      $this.siblings().removeClass('active');
      $this.addClass('active');
      $this.prevAll().addClass('active');
    },

    /**
     * On mouse out.
     */
    starMouseOut: function() {
      var $this = $(this);
      $this.parent().children('.has-sub').addClass('submitted').removeClass('has-sub');
      $this.removeClass('active');
      $this.siblings().removeClass('active');
    },

    sendRating: function (path, index, callback) {
      var url = Drupal.settings.basePath + 'ding_entity_rating/' + path + '/' + index,
        $dummy = $('<a href="' + url + '"></a>'),
        drupal_ajax = new Drupal.ajax('fake', $dummy, {
          url: url,
          event: 'click',
          progress: { type: 'custom' },
        }),
        ofunc_complete = drupal_ajax.options.complete;

      drupal_ajax.options.complete = function (xmlhttprequest, status) {
        callback();
        return ofunc_complete(xmlhttprequest, status);
      };

      drupal_ajax.beforeSerialize(drupal_ajax.element, drupal_ajax.options);

      $.ajax(drupal_ajax.options);
    },

    /**
     * Events bound via _on are removed automatically revert other
     * modifications here.
     */
    _destroy: function() {
      this.element
        .removeClass( "ding-rating" )
        .enableSelection();
    },

    /**
     * _setOptions is called with a hash of all options that are changing.
     */
    _setOptions: function() {
      // _super and _superApply handle keeping the right this-context.
      if (this._superApply) {
        this._superApply( arguments );
      }
      this._refresh();
    },

    /**
     * _setOption is called for each individual option that is changing.
     */
    _setOption: function( key, value ) {
      this._super( key, value );
    }
  });

  // Drupal behavior.
  Drupal.behaviors.ding_entity_rating = {
    attach: function (context) {
      // Attach rating widget.
      $('.ding-entity-rating, .ding-entity-rating-submitted', context).rating();

      var rating_ids = [];
      $('.ding-entity-rating', context).each(function () {
        rating_ids.push($(this).attr('data-ding-entity-rating-id'));
      });

      if (rating_ids.length > 0) {
        $.ajax('/ding_entity_rating/get', {
          data: {ids: rating_ids},
          dataType: 'json',
          method: 'get',
          success: function (data) {
            for (var i in data) {
              if (data[i] !== false) {
                $('.ding-entity-rating[data-ding-entity-rating-id="' + i + '"] .js-rating-symbol')
                  .eq(data[i])
                  .removeClass('submitted')
                  .prevAll().addClass('submitted')
                  .end().nextAll().removeClass('submitted')
                  .end().parent().addClass('has-submission')
                  .find('.ding-entity-rating-avg').remove();
              }
            }
          }
        });
      }
    }
  };
})(jQuery);
;
/**
 * @file
 * JavaScript behaviours for fetching and displaying availability.
 */

(function ($) {
  "use strict";

  // Cache of fetched availability information.
  Drupal.DADB = {};

  Drupal.behaviors.ding_availability = {
    attach: function (context) {
      var settings = Drupal.settings;
      var ids = [];
      var html_ids = [];

      // Loop through the materials given in the settings and collect
      // HTML ids and entity_ids.
      if (settings.hasOwnProperty('ding_availability')) {
        $.each(settings.ding_availability, function (id, entity_ids) {
          $.each(entity_ids, function (index, entity_id) {
            if (Drupal.DADB[entity_id] === undefined) {
              Drupal.DADB[entity_id] = null;
              ids.push(entity_id);
              html_ids.push(id);
            }
          });
        });
      }

      // If there's any reservation buttons, switch to holdings. We have
      // periodicals that's a bit off an odd one in that they can both
      // have issues, which means that the reservation button for the
      // main object should be disabled, or not have any issues, which
      // means that it should be left alone. So we need to fetch full
      // holdings in order to determine whether the material is a
      // periodical. This is a bit of a hack, but it's the quickest way
      // of fixing the problem right now.
      if ($('.reserve-button').size() > 0) {
        settings.ding_availability_mode = 'holdings';
      }

      $.each(html_ids, function (index, id) {
        $('#' + id).addClass('pending');
      });

      // Fetch availability.
      if (ids.length > 0) {
        var mode = settings.ding_availability_mode ? settings.ding_availability_mode : 'items';
        var path = settings.basePath + 'ding_availability/' + mode + '/' + ids.join(',');
        $.ajax({
          dataType: "json",
          url: path,
          success: function (data) {
            $.each(data, function (id, item) {
              // Update cache.
              Drupal.DADB[id] = item;
            });

            $.each(settings.ding_availability, function (id, entity_ids) {
              if (id.match(/^availability-/)) {
                // Update availability indicators.
                ding_availability_update_availability(id, entity_ids);
              }
              else {
                // Update holding information.
                ding_availability_update_holdings(id, entity_ids);
              }
            });
          },
          error: function () {
            $('div.loader').remove();
          }
        });
      }
      else {
        // Apply already fetched availability, if any.
        if (settings.hasOwnProperty('ding_availability')) {
          $.each(settings.ding_availability, function (id, entity_ids) {
            ding_availability_update_availability(id, entity_ids);
          });
        }
      }
    }
  };

  /**
   * Update availability information.
   *
   * Add classes to reservation texts and display reservation button based on
   * availability information.
   *
   * @param id
   *   HTML id of the availability to update.
   * @param entity_ids
   *   Entity id for which to update availability information.
   */
  function ding_availability_update_availability(id, entity_ids) {
    var available = false;
    var reservable = false;
    var element = $('#' + id);
    element.removeClass('pending').addClass('processed');

    $.each(entity_ids, function (index, entity_id) {
      // Reserve button.
      var reserve_button = element.parents('.ting-object:first, .material-item:first').find('a[id$=' + entity_id + '].reserve-button');

      if (Drupal.DADB[entity_id]) {
        var available = available || Drupal.DADB[entity_id]['available'];
        var reservable = reservable || Drupal.DADB[entity_id]['reservable'];

        // Special handling for periodicals.
        if (typeof Drupal.DADB[entity_id]['is_periodical'] !== 'undefined' &&
            Drupal.DADB[entity_id]['is_periodical']) {
          // The main object of a periodical is neither available nor
          // reservable, the individual issues is.
          available = reservable = false;
        }
        var classes = [];

        classes.push(available ? 'available' : 'unavailable');
        classes.push(reservable ? 'reservable' : 'not-reservable');

        $.each(classes, function (i, class_name) {
          element.addClass(class_name);

          // Add class to reserve button.
          if (reserve_button.length) {
            reserve_button.addClass(class_name);
          }
        });

        if (available && !reservable) {
          reserve_button.removeClass('available').addClass('unavailable');
        }
      }
      else {
        reserve_button.addClass('not-reservable');
      }
    });
  }

  /**
   * Update availability holdings information.
   *
   * Insert HTML with information about where the entities are located.
   *
   * @param id
   *   HTML id of the availability to update.
   * @param entity_ids
   *   Entity id for which to update availability holdings information.
   */
  function ding_availability_update_holdings(id, entity_ids) {
    $.each(entity_ids, function (i, entity_id) {
      if (Drupal.DADB[entity_id] && (Drupal.DADB[entity_id]['holdings'])) {
        // Insert/update holding information for material.
        $('#' + id).html(Drupal.DADB[entity_id].html);
      }
    });
  }

})(jQuery);
;

(function($) {

/**
 * Drupal FieldGroup object.
 */
Drupal.FieldGroup = Drupal.FieldGroup || {};
Drupal.FieldGroup.Effects = Drupal.FieldGroup.Effects || {};
Drupal.FieldGroup.groupWithfocus = null;

Drupal.FieldGroup.setGroupWithfocus = function(element) {
  element.css({display: 'block'});
  Drupal.FieldGroup.groupWithfocus = element;
}

/**
 * Implements Drupal.FieldGroup.processHook().
 */
Drupal.FieldGroup.Effects.processFieldset = {
  execute: function (context, settings, type) {
    if (type == 'form') {
      // Add required fields mark to any fieldsets containing required fields
      $('fieldset.fieldset', context).once('fieldgroup-effects', function(i) {
        if ($(this).is('.required-fields') && $(this).find('.form-required').length > 0) {
          $('legend span.fieldset-legend', $(this)).eq(0).append(' ').append($('.form-required').eq(0).clone());
        }
        if ($('.error', $(this)).length) {
          $('legend span.fieldset-legend', $(this)).eq(0).addClass('error');
          Drupal.FieldGroup.setGroupWithfocus($(this));
        }
      });
    }
  }
}

/**
 * Implements Drupal.FieldGroup.processHook().
 */
Drupal.FieldGroup.Effects.processAccordion = {
  execute: function (context, settings, type) {
    $('div.field-group-accordion-wrapper', context).once('fieldgroup-effects', function () {
      var wrapper = $(this);

      // Get the index to set active.
      var active_index = false;
      wrapper.find('.accordion-item').each(function(i) {
        if ($(this).hasClass('field-group-accordion-active')) {
          active_index = i;
        }
      });

      wrapper.accordion({
        heightStyle: "content",
        active: active_index,
        collapsible: true,
        changestart: function(event, ui) {
          if ($(this).hasClass('effect-none')) {
            ui.options.animated = false;
          }
          else {
            ui.options.animated = 'slide';
          }
        }
      });

      if (type == 'form') {

        var $firstErrorItem = false;

        // Add required fields mark to any element containing required fields
        wrapper.find('div.field-group-accordion-item').each(function(i) {

          if ($(this).is('.required-fields') && $(this).find('.form-required').length > 0) {
            $('h3.ui-accordion-header a').eq(i).append(' ').append($('.form-required').eq(0).clone());
          }
          if ($('.error', $(this)).length) {
            // Save first error item, for focussing it.
            if (!$firstErrorItem) {
              $firstErrorItem = $(this).parent().accordion("activate" , i);
            }
            $('h3.ui-accordion-header').eq(i).addClass('error');
          }
        });

        // Save first error item, for focussing it.
        if (!$firstErrorItem) {
          $('.ui-accordion-content-active', $firstErrorItem).css({height: 'auto', width: 'auto', display: 'block'});
        }

      }
    });
  }
}

/**
 * Implements Drupal.FieldGroup.processHook().
 */
Drupal.FieldGroup.Effects.processHtabs = {
  execute: function (context, settings, type) {
    if (type == 'form') {
      // Add required fields mark to any element containing required fields
      $('fieldset.horizontal-tabs-pane', context).once('fieldgroup-effects', function(i) {
        if ($(this).is('.required-fields') && $(this).find('.form-required').length > 0) {
          $(this).data('horizontalTab').link.find('strong:first').after($('.form-required').eq(0).clone()).after(' ');
        }
        if ($('.error', $(this)).length) {
          $(this).data('horizontalTab').link.parent().addClass('error');
          Drupal.FieldGroup.setGroupWithfocus($(this));
          $(this).data('horizontalTab').focus();
        }
      });
    }
  }
}

/**
 * Implements Drupal.FieldGroup.processHook().
 */
Drupal.FieldGroup.Effects.processTabs = {
  execute: function (context, settings, type) {
    if (type == 'form') {

      var errorFocussed = false;

      // Add required fields mark to any fieldsets containing required fields
      $('fieldset.vertical-tabs-pane', context).once('fieldgroup-effects', function(i) {
        if ($(this).is('.required-fields') && $(this).find('.form-required').length > 0) {
          $(this).data('verticalTab').link.find('strong:first').after($('.form-required').eq(0).clone()).after(' ');
        }
        if ($('.error', $(this)).length) {
          $(this).data('verticalTab').link.parent().addClass('error');
          // Focus the first tab with error.
          if (!errorFocussed) {
            Drupal.FieldGroup.setGroupWithfocus($(this));
            $(this).data('verticalTab').focus();
            errorFocussed = true;
          }
        }
      });
    }
  }
}

/**
 * Implements Drupal.FieldGroup.processHook().
 *
 * TODO clean this up meaning check if this is really
 *      necessary.
 */
Drupal.FieldGroup.Effects.processDiv = {
  execute: function (context, settings, type) {

    $('div.collapsible', context).once('fieldgroup-effects', function() {
      var $wrapper = $(this);

      // Turn the legend into a clickable link, but retain span.field-group-format-toggler
      // for CSS positioning.

      var $toggler = $('span.field-group-format-toggler:first', $wrapper);
      var $link = $('<a class="field-group-format-title" href="#"></a>');
      $link.prepend($toggler.contents());

      // Add required field markers if needed
      if ($(this).is('.required-fields') && $(this).find('.form-required').length > 0) {
        $link.append(' ').append($('.form-required').eq(0).clone());
      }

      $link.appendTo($toggler);

      // .wrapInner() does not retain bound events.
      $link.click(function () {
        var wrapper = $wrapper.get(0);
        // Don't animate multiple times.
        if (!wrapper.animating) {
          wrapper.animating = true;
          var speed = $wrapper.hasClass('speed-fast') ? 300 : 1000;
          if ($wrapper.hasClass('effect-none') && $wrapper.hasClass('speed-none')) {
            $('> .field-group-format-wrapper', wrapper).toggle();
          }
          else if ($wrapper.hasClass('effect-blind')) {
            $('> .field-group-format-wrapper', wrapper).toggle('blind', {}, speed);
          }
          else {
            $('> .field-group-format-wrapper', wrapper).toggle(speed);
          }
          wrapper.animating = false;
        }
        $wrapper.toggleClass('collapsed');
        return false;
      });

    });
  }
};

/**
 * Behaviors.
 */
Drupal.behaviors.fieldGroup = {
  attach: function (context, settings) {
    settings.field_group = settings.field_group || Drupal.settings.field_group;
    if (settings.field_group == undefined) {
      return;
    }

    // Execute all of them.
    $.each(Drupal.FieldGroup.Effects, function (func) {
      // We check for a wrapper function in Drupal.field_group as
      // alternative for dynamic string function calls.
      var type = func.toLowerCase().replace("process", "");
      if (settings.field_group[type] != undefined && $.isFunction(this.execute)) {
        this.execute(context, settings, settings.field_group[type]);
      }
    });

    // Fixes css for fieldgroups under vertical tabs.
    $('.fieldset-wrapper .fieldset > legend').css({display: 'block'});
    $('.vertical-tabs fieldset.fieldset').addClass('default-fallback');

    // Add a new ID to each fieldset.
    $('.group-wrapper .horizontal-tabs-panes > fieldset', context).once('group-wrapper-panes-processed', function() {
      // Tats bad, but we have to keep the actual id to prevent layouts to break.
      var fieldgroupID = 'field_group-' + $(this).attr('id');
      $(this).attr('id', fieldgroupID);
    });
    // Set the hash in url to remember last userselection.
    $('.group-wrapper ul li').once('group-wrapper-ul-processed', function() {
      var fieldGroupNavigationListIndex = $(this).index();
      $(this).children('a').click(function() {
        var fieldset = $('.group-wrapper fieldset').get(fieldGroupNavigationListIndex);
        // Grab the first id, holding the wanted hashurl.
        var hashUrl = $(fieldset).attr('id').replace(/^field_group-/, '').split(' ')[0];
        window.location.hash = hashUrl;
      });
    });

  }
};

})(jQuery);
;
(function($) {
  "use strict";

  // Helper function to get information about a given cover place holder.
  var ting_covers_extract_data = function(e) {
    var id = $(e).data('ting-cover-object-id');
    var image_style = $(e).data('ting-cover-style');
    if (!id) {
      return false;
    }
    return {
      id : id,
      image_style : image_style
    };
  };

  var ting_cover_insert = function(covers) {
    $.each(covers, function(index, cover_info) {
      $('.ting-cover-processing[data-ting-cover-object-id="' + cover_info.id + '"][data-ting-cover-style="' + cover_info.image_style + '"').html('<img src="' + cover_info.url + '"/>');
    });
  };

  Drupal.behaviors.tingCovers = {
    attach: function(context) {
      // Assemble information regarding covers.
      var cover_data = [];

      // Extract cover information from the dom.
      $('.ting-cover:not(.ting-cover-processing, .ting-cover-processed)', context).each(function(index, element) {
        cover_data.push(ting_covers_extract_data(element));
      }).addClass('ting-cover-processing');

      if (cover_data.length > 0) {
        //Retrieve covers
        var request = $.ajax({
          url: Drupal.settings.basePath + 'ting/covers',
          type: 'POST',
          data: {
            coverData: cover_data
          },
          dataType: 'json',
          success: ting_cover_insert,
          // Update processing state.
          complete: function(request, status) {
            var processing = $('.ting-cover-processing', context);
            if (status === 'success') {
              processing.addClass('ting-cover-processed');
            }
            processing.removeClass('ting-cover-processing');
          }
        });

        // Associate the request with the context so we can abort the request if
        // the context is detached removed before completion.
        $(context).data('request', request);
      }
    },
    detach: function(context) {
      // If we have a request associated with the context then abort it.
      var request = $(context).data('request');
      if (request) {
        request.abort();
      }
    }
  };
}(jQuery));
;
(function ($) {

$(document).ready(function() {

  // Expression to check for absolute internal links.
  var isInternal = new RegExp("^(https?):\/\/" + window.location.host, "i");

  // Attach onclick event to document only and catch clicks on all elements.
  $(document.body).click(function(event) {
    // Catch the closest surrounding link of a clicked element.
    $(event.target).closest("a,area").each(function() {

      var ga = Drupal.settings.googleanalytics;
      // Expression to check for special links like gotwo.module /go/* links.
      var isInternalSpecial = new RegExp("(\/go\/.*)$", "i");
      // Expression to check for download links.
      var isDownload = new RegExp("\\.(" + ga.trackDownloadExtensions + ")$", "i");

      // Is the clicked URL internal?
      if (isInternal.test(this.href)) {
        // Skip 'click' tracking, if custom tracking events are bound.
        if ($(this).is('.colorbox')) {
          // Do nothing here. The custom event will handle all tracking.
        }
        // Is download tracking activated and the file extension configured for download tracking?
        else if (ga.trackDownload && isDownload.test(this.href)) {
          // Download link clicked.
          var extension = isDownload.exec(this.href);
          _gaq.push(["_trackEvent", "Downloads", extension[1].toUpperCase(), this.href.replace(isInternal, '')]);
        }
        else if (isInternalSpecial.test(this.href)) {
          // Keep the internal URL for Google Analytics website overlay intact.
          _gaq.push(["_trackPageview", this.href.replace(isInternal, '')]);
        }
      }
      else {
        if (ga.trackMailto && $(this).is("a[href^='mailto:'],area[href^='mailto:']")) {
          // Mailto link clicked.
          _gaq.push(["_trackEvent", "Mails", "Click", this.href.substring(7)]);
        }
        else if (ga.trackOutbound && this.href.match(/^\w+:\/\//i)) {
          if (ga.trackDomainMode == 2 && isCrossDomain($(this).attr('hostname'), ga.trackCrossDomains)) {
            // Top-level cross domain clicked. document.location is handled by _link internally.
            event.preventDefault();
            _gaq.push(["_link", this.href]);
          }
          else {
            // External link clicked.
            _gaq.push(["_trackEvent", "Outbound links", "Click", this.href]);
          }
        }
      }
    });
  });

  // Colorbox: This event triggers when the transition has completed and the
  // newly loaded content has been revealed.
  $(document).bind("cbox_complete", function() {
    var href = $.colorbox.element().attr("href");
    if (href) {
      _gaq.push(["_trackPageview", href.replace(isInternal, '')]);
    }
  });

});

/**
 * Check whether the hostname is part of the cross domains or not.
 *
 * @param string hostname
 *   The hostname of the clicked URL.
 * @param array crossDomains
 *   All cross domain hostnames as JS array.
 *
 * @return boolean
 */
function isCrossDomain(hostname, crossDomains) {
  /**
   * jQuery < 1.6.3 bug: $.inArray crushes IE6 and Chrome if second argument is
   * `null` or `undefined`, http://bugs.jquery.com/ticket/10076,
   * https://github.com/jquery/jquery/commit/a839af034db2bd934e4d4fa6758a3fed8de74174
   *
   * @todo: Remove/Refactor in D8
   */
  if (!crossDomains) {
    return false;
  }
  else {
    return $.inArray(hostname, crossDomains) > -1 ? true : false;
  }
}

})(jQuery);
;
